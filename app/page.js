import HomePage from "../components/HomePage";

export const metadata = {
  title: "MoveOps | Spain execution partner for agencies (retail + stands)",
  description: "White-label on-site execution in Spain: retail installs, POS/graphics, stand build support, photo reporting.",
  alternates: { canonical: "https://moveops.services" },
};

export default function Page() {
  return <HomePage />;
}
